"use client";

import { useMemo, useState } from "react";
import { Check, Code2, GitCompareArrows, Lightbulb, RefreshCcw, ShieldCheck } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const equalityExamples = [
  ["String and number", `5 == "5"     // true\n5 === "5"   // false`, "Loose equality converts the string; strict equality sees different types."],
  ["Boolean and number", `true == 1    // true\ntrue === 1  // false\nfalse == 0   // true`, "Loose equality converts true to 1 and false to 0 before comparing."],
  ["null and undefined", `null == undefined     // true\nnull === undefined   // false`, "This is a special rule of loose equality. Strict equality requires the same type."],
  ["Objects", `{} == {}       // false\nconst a = {};\nconst b = a;\na === b        // true`, "Objects are compared by reference. Two separate object literals are different objects."],
];

const quizQuestions = [
  { question: "What does == perform when operand types differ?", options: ["No comparison", "Type coercion may occur", "Only reference comparison", "It always throws"], answer: 1, explanation: "Loose equality may convert one operand before comparing the values." },
  { question: "What is the result of \"5\" === 5?", options: ["true", "false", "undefined", "TypeError"], answer: 1, explanation: "The values represent the same number, but one operand is a string and the other is a number." },
  { question: "Why is true == 1 true?", options: ["true is converted to 1", "1 is converted to true only by ===", "Objects share a reference", "The comparison is invalid"], answer: 0, explanation: "Loose equality converts true to the numeric value 1 in this comparison." },
  { question: "What is the result of null == undefined?", options: ["true", "false", "null", "undefined"], answer: 0, explanation: "JavaScript has a special loose-equality rule that treats null and undefined as equal to each other." },
  { question: "Which operator is preferred for predictable production code?", options: ["=", "==", "===", "!= only"], answer: 2, explanation: "=== compares both value and type without implicit coercion, making intent clearer." },
  { question: "What does {} === {} return?", options: ["true", "false", "object", "SyntaxError"], answer: 1, explanation: "Each object literal creates a different object reference, so they are not the same object." },
  { question: "Which expression intentionally checks for both null and undefined?", options: ["value === null", "value == null", "value === undefined", "Boolean(value)"], answer: 1, explanation: "value == null is one accepted intentional use of loose equality because it matches only null or undefined." },
  { question: "What is the safer way to compare a form age value with 18?", options: ["age == 18", "Number(age) === 18", "age = 18", "String(age) == true"], answer: 1, explanation: "Convert the input explicitly, then use strict equality so the comparison is predictable." },
];

function CodeExample({ title, code, explanation }: { title: string; code: string; explanation: string }) {
  return <Card className="border-white/10 bg-card/60"><CardHeader><CardTitle className="text-lg">{title}</CardTitle></CardHeader><CardContent><pre className="overflow-auto rounded-lg border border-white/10 bg-background/80 p-4 font-code text-xs leading-6 text-muted-foreground"><code>{code}</code></pre><p className="mt-4 text-sm leading-6 text-muted-foreground">{explanation}</p></CardContent></Card>;
}

function EqualityPlayground() {
  const [left, setLeft] = useState("5");
  const [right, setRight] = useState("5");
  // Intentional loose comparison: this playground demonstrates the == operator.
  // eslint-disable-next-line eqeqeq
  const loose = left == right;
  const strict = left === right;
  return <Card className="border-primary/20 bg-primary/5"><CardHeader><CardTitle className="flex items-center gap-2 font-headline text-2xl"><GitCompareArrows className="h-5 w-5 text-primary" />Try the comparison</CardTitle></CardHeader><CardContent className="space-y-5"><p className="text-sm leading-6 text-muted-foreground">Edit the two string inputs. The loose comparison uses JavaScript coercion; the strict comparison checks the strings exactly as entered.</p><div className="grid gap-4 md:grid-cols-2"><label className="space-y-2 text-sm font-semibold">Left value<input value={left} onChange={(event) => setLeft(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-white/10 bg-background/70 px-3 font-code text-sm outline-none focus:border-primary/50" /></label><label className="space-y-2 text-sm font-semibold">Right value<input value={right} onChange={(event) => setRight(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-white/10 bg-background/70 px-3 font-code text-sm outline-none focus:border-primary/50" /></label></div><div className="grid gap-3 md:grid-cols-2"><div className={`rounded-lg border p-4 font-code text-sm ${loose ? "border-success/50 bg-success/10" : "border-destructive/40 bg-destructive/10"}`}>{JSON.stringify(left)} == {JSON.stringify(right)} → {String(loose)}</div><div className={`rounded-lg border p-4 font-code text-sm ${strict ? "border-success/50 bg-success/10" : "border-destructive/40 bg-destructive/10"}`}>{JSON.stringify(left)} === {JSON.stringify(right)} → {String(strict)}</div></div></CardContent></Card>;
}

function Quiz() {
  const [answers, setAnswers] = useState<Record<number, number>>({});
  const score = useMemo(() => quizQuestions.reduce((total, question, index) => total + (answers[index] === question.answer ? 1 : 0), 0), [answers]);
  return <section className="space-y-5"><div><Badge variant="outline" className="border-primary/20 bg-primary/5 text-primary">Practice quiz</Badge><h2 className="mt-3 font-headline text-3xl font-bold">== vs === Quiz</h2><p className="mt-2 text-sm leading-7 text-muted-foreground">Select an answer to see the explanation.</p></div>{quizQuestions.map((question, index) => <Card key={question.question} className="border-white/10 bg-card/70"><CardContent className="space-y-4 p-5"><p className="font-semibold leading-6">{index + 1}. {question.question}</p><div className="grid gap-2">{question.options.map((option, optionIndex) => { const selected = answers[index] === optionIndex; const correct = optionIndex === question.answer; return <button key={option} type="button" onClick={() => setAnswers((current) => ({ ...current, [index]: optionIndex }))} className={`rounded-lg border p-3 text-left text-sm transition-colors ${selected ? (correct ? "border-success/50 bg-success/10" : "border-destructive/50 bg-destructive/10") : "border-white/10 bg-background/30 hover:border-primary/30"}`}><span className="mr-2 font-semibold">{String.fromCharCode(65 + optionIndex)}.</span>{option}{selected ? <span className="mt-2 block border-t border-white/10 pt-2 text-xs leading-5 text-muted-foreground">{question.explanation}</span> : null}</button>; })}</div></CardContent></Card>)}<Card className="border-primary/20 bg-primary/5"><CardContent className="flex flex-col gap-3 p-5 md:flex-row md:items-center md:justify-between"><p className="font-semibold">Score: {score} / {quizQuestions.length}</p><Button variant="outline" className="rounded-full border-white/10" onClick={() => setAnswers({})}><RefreshCcw className="mr-2 h-4 w-4" />Reset Quiz</Button></CardContent></Card></section>;
}

export function JavaScriptEqualityLesson() {
  return <div className="relative overflow-hidden"><div className="absolute inset-0 hero-gradient -z-10" /><section className="container mx-auto px-4 pb-16 pt-28"><div className="mx-auto max-w-4xl text-center"><Badge variant="outline" className="mb-5 rounded-full border-primary/20 bg-primary/5 px-4 py-1 text-primary"><Code2 className="mr-2 h-3.5 w-3.5" />JavaScript Fundamentals</Badge><h1 className="text-5xl font-headline leading-tight lg:text-7xl"><span className="gradient-text">== vs ===</span></h1><p className="mx-auto mt-6 max-w-3xl text-xl leading-relaxed text-muted-foreground">Understand loose equality, strict equality, type coercion, and predictable JavaScript comparisons.</p><p className="mx-auto mt-5 max-w-3xl text-base leading-8 text-muted-foreground">Learn when JavaScript converts values, why production code usually prefers ===, how null and undefined behave, and why objects are compared by reference.</p></div></section><section className="container mx-auto space-y-10 px-4 pb-24"><Card className="border-primary/20 bg-card/60 backdrop-blur-xl"><CardContent className="space-y-4 p-6"><div className="flex items-center gap-2 text-primary"><Lightbulb className="h-5 w-5" /><h2 className="font-headline text-2xl font-bold">The core difference</h2></div><div className="grid gap-5 md:grid-cols-2"><div className="rounded-xl border border-amber-400/30 bg-amber-400/5 p-5"><h3 className="font-headline text-xl font-bold">== Loose Equality</h3><p className="mt-3 text-sm leading-7 text-muted-foreground">Compares values and may perform type coercion before comparing them. For example, "5" == 5 is true because the string can be converted to the number 5.</p></div><div className="rounded-xl border border-primary/30 bg-primary/5 p-5"><h3 className="font-headline text-xl font-bold">=== Strict Equality</h3><p className="mt-3 text-sm leading-7 text-muted-foreground">Compares both value and data type without automatic conversion. "5" === 5 is false because a string and a number are different types.</p></div></div><div className="rounded-lg border border-white/10 bg-background/40 p-4 text-sm leading-6 text-muted-foreground"><span className="font-semibold text-foreground">Analogy: </span>== accepts information that looks similar after conversion. === requires the value and type to match exactly.</div></CardContent></Card><div className="grid gap-6 lg:grid-cols-2">{equalityExamples.map(([title, code, explanation]) => <CodeExample key={title} title={title} code={code} explanation={explanation} />)}</div><EqualityPlayground /><Card className="overflow-hidden border-white/10 bg-card/70"><CardHeader><CardTitle className="font-headline text-2xl">Equality Comparison Table</CardTitle></CardHeader><CardContent className="overflow-x-auto p-0"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-primary/10"><tr>{["Expression", "==", "===", "Reason"].map((column) => <th key={column} className="px-4 py-3 font-semibold">{column}</th>)}</tr></thead><tbody>{[["\"5\" and 5", "true", "false", "String converted by =="], ["true and 1", "true", "false", "true becomes 1"], ["false and 0", "true", "false", "false becomes 0"], ["null and undefined", "true", "false", "Special loose-equality rule"], ["{} and {}", "false", "false", "Different object references"]].map((row) => <tr key={row[0]} className="border-t border-white/10">{row.map((cell, index) => <td key={`${row[0]}-${index}`} className={`px-4 py-3 ${index === 0 ? "font-semibold" : "text-muted-foreground"}`}>{cell}</td>)}</tr>)}</tbody></table></CardContent></Card><Card className="border-success/20 bg-success/10"><CardContent className="space-y-4 p-6"><div className="flex items-center gap-2 text-success"><ShieldCheck className="h-5 w-5" /><h2 className="font-headline text-2xl font-bold">Best Practices and Interview Answer</h2></div><div className="grid gap-3 md:grid-cols-2">{["Prefer === and !== in almost all situations.", "Convert input explicitly with Number(), String(), or Boolean().", "Use value == null intentionally when matching null or undefined.", "Do not rely on user input coercion in production logic.", "Objects compare references, not their visible properties.", "Most ESLint configurations recommend strict equality."].map((item) => <div key={item} className="flex gap-2 rounded-lg border border-white/10 bg-background/40 p-4 text-sm leading-6 text-muted-foreground"><Check className="mt-1 h-4 w-4 shrink-0 text-success" />{item}</div>)}</div><p className="rounded-lg border border-white/10 bg-background/40 p-4 text-sm leading-7 text-muted-foreground"><span className="font-semibold text-foreground">30-second answer: </span>== performs loose equality and may convert operand types. === performs strict equality by checking both value and type without conversion, so it is usually more predictable in production code.</p></CardContent></Card><Card className="border-white/10 bg-card/70"><CardHeader><CardTitle className="font-headline text-2xl">Hands-on Exercises</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2">{[["Predict the output", `console.log("10" == 10);\nconsole.log("10" === 10);`], ["Explain the difference", `console.log(false == "");\nconsole.log(false === "");`], ["Special null case", `console.log(null == undefined);\nconsole.log(null === undefined);`], ["Write a safe comparator", `function areEqual(value1, value2) {\n  return value1 === value2;\n}`]].map(([title, code]) => <CodeExample key={title} title={title} code={code} explanation="Work out the result first, then use explicit conversion and strict equality where appropriate." />)}</CardContent></Card><Quiz /></section></div>;
}
